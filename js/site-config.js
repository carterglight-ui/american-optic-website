/* ============================================================
   American Optic Media — site configuration
   Edit these values, then everything on the site updates.
   ============================================================ */
window.AO_CONFIG = {
  // --- Business contact (shown in footer, CTAs, the quote page) ---
  email:      "Carter.g.light@gmail.com",
  phone:      "(904) 608-4693",
  phoneHref:  "+19046084693",                // digits only, with country code
  location:   "Jacksonville, Florida",       // TODO: confirm service area — unconfirmed placeholder

  // --- Optional links ---
  // If you have a hosted reel (YouTube / Vimeo / MP4), paste the URL here and a
  // "Watch Reel" button will appear in the hero. Leave "" to hide it.
  reelUrl:    "",
  // If you use a scheduling tool (Calendly, etc.), paste the link here.
  // Leave "" to fall back to the quote page.
  calendarUrl: "",

  // --- Quote form delivery ---
  // Out of the box (leave this "") the quote form opens the visitor's email
  // app with everything pre-filled to `email` above — no setup needed, but
  // THEY have to hit send.
  // For the form to submit straight to your inbox with no email app involved,
  // sign up free at https://formspree.io, create a form pointed at your email,
  // confirm the verification email, then paste the endpoint it gives you here,
  // e.g. "https://formspree.io/f/abcdwxyz". See README.md for the 2-minute walkthrough.
  formEndpoint: ""
};
